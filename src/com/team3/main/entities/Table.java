package com.team3.main.entities;

public class Table extends Obstacle {

    // Standard sized obstacle, permeable with leg colliders
    public Table(int x, int y) {
        super(x, y, true);
    }
}
